{
  "name": "video-feed",
  "version": "1.0.0",
  "description": "test application for displaying video feed",
  "scripts": {
    "setup": "echo 'Nothing to setup'",
    "start": "npm run build:dev",
    "build": "npm run build:umd && npm run build:min",
    "build:dev": "npm run build:umd -- --livereload",
    "build:umd": "bt build -t js -c build/webpack.config.js",
    "build:min": "npm run build:umd -- --min",
    "clean": "bt clean",
    "doc": "echo 'doc task not available'",
    "lint": "bt lint -t js -f \"app/**/*.js\" \"src/**/*.js\" \"test/specs/**/*.js\" \"test/**/*.js\"",
    "test": "npm run test:unit && npm run test:functional && npm run test:integration && npm run test:e2e",
    "test:e2e": "echo 'task test:e2e is not defined'",
    "test:functional": "echo 'task test:functional is not defined'",
    "test:integration": "echo 'task test:integration is not defined'",
    "test:unit": "bt test -t unit -s js -w build/webpack.config.js -c test/karma.config.js -o test/results",
    "test:unit:umd": "bt test -t unit -s js -w build/webpack.config.js -c test/karma.config.js -o test/results",
    "test:coverage": "npm run test:coverage:unit && npm run test:coverage:functional && npm run test:coverage:integration && npm run test:coverage:e2e",
    "test:coverage:e2e": "echo 'task test:coverage:e2e is not defined'",
    "test:coverage:functional": "echo 'task test:coverage:functional is not defined'",
    "test:coverage:integration": "echo 'task test:coverage:integration is not defined'",
    "test:coverage:unit": "npm run test:unit -- --cov"
  },
  "keywords": ["video", "feed", "media"],
  "author": {
    "name": "vira-lytvyn",
    "email": "baterfluy@gmail.com"
  },
  "license": "MIT",
  "devDependencies": {
    "angular-mocks": "^1.6.4",
    "build-tool": "0.0.1",
    "css-loader": "^0.28.4",
    "extract-text-webpack-plugin": "^2.1.2",
    "html-loader": "^0.4.5",
    "karma-spec-reporter": "0.0.31",
    "less": "^2.7.2",
    "less-loader": "^4.0.4",
    "ng-annotate-loader": "^0.6.1",
    "style-loader": "^0.18.2"
  },
  "dependencies": {
    "angular": "^1.6.4"
  }
}
